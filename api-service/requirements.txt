# ============================
# 上课摸鱼搭子 - 后端依赖清单
# ============================

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 跨域支持
python-multipart==0.0.20

# PPT 解析
python-pptx==1.0.2

# PDF 解析
pypdf>=4.0.0

# Word 文档解析
python-docx>=1.1.0

# 音频录制（可选）
# 默认依赖不强制安装 PyAudio，便于 webspeech/browser-only 模式开箱可用。
# 需要麦克风采集模式（local / dashscope / seed-asr）时，请额外安装 requirements-mic.txt。

# OpenAI Compatible API 调用
openai==1.58.1

# 环境变量
python-dotenv==1.0.1

# ASR - 阿里云百炼 DashScope
dashscope>=1.20.0

# ASR - 字节跳动 Seed-ASR (WebSocket 客户端)
websocket-client>=1.7.0

# WebSocket 支持 (FastAPI 自带，但需要 websockets 库)
websockets==14.1

# 环境变量管理
python-dotenv==1.0.1

# ASR - 本地免费语音识别 (Google 免费 API，无需密钥)
SpeechRecognition>=3.10.0

# 异步文件操作
aiofiles==24.1.0
