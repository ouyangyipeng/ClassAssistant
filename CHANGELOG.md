# 更新记录

## 2.0.1 — 2026-09-23

- 将应用标题栏、启动页与 macOS/Windows 安装图标中的默认 Tauri 标识替换为 ClassFox 狐狸标识。
- 重新设计 README，加入使用合成课堂数据采集的真实应用截图。
- 发布暖白极简的 ClassFox 2.0 产品主页，接入 `class.nexa-lang.com`；技术文档保留原有内容路径，入口为 `/guide/`。
- 课堂数据、模型配置和 API 行为保持 2.0.0 兼容，微信正式交付仍计划于 v2.5。

## 2.0.0 — 2026-09-23

### 课堂与桌面

- 按课堂保存原文、资料和笔记，改用带事务的 SQLite 存储；显式导入旧文件，保留原件。
- 重建完整工作区与紧凑窗口，增加原文搜索、历史分页、资料引用、Markdown 展示与原生导出。
- 救场、进度、追问和总结支持流式显示、取消、完整性检查与耗时统计；只有完整成功的总结进入笔记。
- 设置分离非秘密偏好与系统凭据；加入服务商预设、固定短文本连接测试和旧外观导入。

### 模型与运行稳定性

- 下载和校验受管离线 ASR、VAD、LLM 与运行时；支持安装取消、续传、失败重试和课前预热。
- 重写音频生命周期，启动失败、权限错误、断网、取消及迟到回调都有明确状态。
- 资料解析进入独立进程，支持体积与解压限制、超时和取消清理。
- Windows/macOS 使用用户数据目录及本实例子进程管理，移除按名称、端口终止进程的旧逻辑。

### v2.5 开发预览（不属于本次正式交付）

- 原生课堂、回顾和设置页面，手机独立 BYOK 与安全连接电脑两条路径。
- 手机 Key 仅在内存保存；后台暂停录音和生成，不自动恢复采集。
- 独立课堂权限、短时配对、双端核对、加密计数和同密文重试，支持撤销。
- 本地行为测试、真实回环协议互通与构建产物验证；微信开发者工具、真机、AppID 和上架尚待资源与验证。

### 兼容变化

- 移除旧无鉴权 `/api/*` 业务接口；桌面使用 `/api/v2`，HTTP 与 WebSocket 均验证身份。只读 `/api/health` 保留。
- 旧 `.env` 不自动读取或覆盖，不再分发共享试用 Key。
- 旧名为 `local` 的 Google 在线识别不再称为离线，仍提供明确标记的兼容路径。离线使用 SenseVoice。
- `.ppt`、`.doc` 旧二进制格式需先转换为 `.pptx`、`.docx`；不伪报解析成功。
- 安装包平台、签名与验证状态分别记录；临时签名不等于 Apple Developer ID 签名或公证。

历史版本和附件保留在 [GitHub Releases](https://github.com/ouyangyipeng/ClassAssistant/releases)。
